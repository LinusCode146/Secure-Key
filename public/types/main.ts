export type PasswordData = {
    password: string
}

export type Password = {
    id?: string
    password: string
    description: string
    masterPassword: string
}

export type Masterpassword = {
    globalMasterPassword: string
}